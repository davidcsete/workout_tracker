<% content_for :title, "New exercise" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New exercise</h1>

  <%= render "form", exercise: @exercise %>

  <%= link_to "Back to workout plan", edit_workout_plan_path(@workout_plan), class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 btn btn-secondary inline-block font-medium" %>
</div>
